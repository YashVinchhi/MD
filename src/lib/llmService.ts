// Mock implementation of getPrediction function
export async function getPrediction(content: string): Promise<string> {
  // Replace this with actual logic to interact with the LLM
  return `Predicted continuation for: ${content}`;
}